services:
  backend:
    env_file:
      - .env
    build:
      context: ./docker/prod
      dockerfile: ./docker/prod/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
    networks:
      - server-network
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: always
    networks:
      - server-network

volumes:
  redis-data:
    driver: local

networks:
  server-network:
    external: true
    driver: bridge