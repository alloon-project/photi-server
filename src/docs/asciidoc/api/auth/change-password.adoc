[[change-password]]
=== 비밀번호 변경

==== HTTP Request
include::{snippets}/auth/change-password/http-request.adoc[]
include::{snippets}/auth/change-password/request-fields.adoc[]

==== HTTP Response
include::{snippets}/auth/change-password/http-response.adoc[]
include::{snippets}/auth/change-password/response-fields.adoc[]

==== Exception

===== 400
----
{
  "code" : "PASSWORD_FIELD_REQUIRED",
  "message" : "비밀번호는 필수 입력입니다."
}
----
----
{
  "code" : "NEW_PASSWORD_FIELD_REQUIRED",
  "message" : "새 비밀번호는 필수 입력입니다."
}
----
----
{
  "code" : "NEW_PASSWORD_LENGTH_INVALID",
  "message" : "비밀번호는 8~30자만 가능합니다."
}
----
----
{
  "code" : "NEW_PASSWORD_FORMAT_INVALID",
  "message" : "비밀번호는 영어, 숫자, 특수문자(#$@!%&*)의 조합으로 입력해 주세요."
}
----
----
{
  "code" : "NEW_PASSWORD_RE_ENTER_FIELD_REQUIRED",
  "message" : "새 비밀번호 재입력은 필수 입력입니다."
}
----
----
{
  "code" : "PASSWORD_MATCH_INVALID",
  "message" : "비밀번호와 비밀번호 재입력이 동일하지 않습니다."
}
----

===== 401
----
{
  "code" : "TOKEN_UNAUTHENTICATED",
  "message" : "승인되지 않은 요청입니다. 다시 로그인 해주세요."
}
----
----
{
  "code" : "LOGIN_UNAUTHENTICATED",
  "message" : "아이디 또는 비밀번호가 틀렸습니다."
}
----

===== 403
----
{
  "code" : "TOKEN_UNAUTHORIZED",
  "message" : "권한이 없는 요청입니다. 로그인 후에 다시 시도 해주세요."
}
----
