[[register]]
=== 회원 가입

==== HTTP Request
include::{snippets}/auth/register/http-request.adoc[]
include::{snippets}/auth/register/request-fields.adoc[]

==== HTTP Response
include::{snippets}/auth/register/http-response.adoc[]
include::{snippets}/auth/register/response-fields.adoc[]

==== Exception

===== 400
----
{
  "code" : "EMAIL_FIELD_REQUIRED",
  "message" : "이메일은 필수 입력입니다."
}
----
----
{
  "code" : "EMAIL_LENGTH_INVALID",
  "message" : "이메일은 1~100자만 가능합니다."
}
----
----
{
  "code" : "EMAIL_FORMAT_INVALID",
  "message" : "올바른 이메일 형식을 입력해 주세요."
}
----
----
{
  "code" : "VERIFICATION_CODE_FIELD_REQUIRED",
  "message" : "이메일 인증코드는 필수 입력입니다."
}
----
----
{
  "code" : "USERNAME_FIELD_REQUIRED",
  "message" : "아이디는 필수 입력입니다."
}
----
----
{
  "code" : "USERNAME_LENGTH_INVALID",
  "message" : "아이디는 5~20자만 가능합니다."
}
----
----
{
  "code" : "USERNAME_FORMAT_INVALID",
  "message" : "아이디는 소문자 영어, 숫자, 특수문자(_)의 조합으로 입력해 주세요."
}
----
----
{
  "code" : "PASSWORD_FIELD_REQUIRED",
  "message" : "비밀번호는 필수 입력입니다."
}
----
----
{
  "code" : "PASSWORD_LENGTH_INVALID",
  "message" : "비밀번호는 8~30자만 가능합니다."
}
----
----
{
  "code" : "PASSWORD_FORMAT_INVALID",
  "message" : "비밀번호는 영어, 숫자, 특수문자(#$@!%&*)의 조합으로 입력해 주세요."
}
----
----
{
  "code" : "PASSWORD_RE_ENTER_FIELD_REQUIRED",
  "message" : "비밀번호 재입력은 필수 입력입니다."
}
----
----
{
  "code" : "EMAIL_VALIDATION_INVALID",
  "message" : "이메일 인증을 먼저 해주세요."
}
----
----
{
  "code" : "PASSWORD_MATCH_INVALID",
  "message" : "비밀번호와 비밀번호 재입력이 동일하지 않습니다."
}
----

===== 409
----
{
  "code" : "EXISTING_USER",
  "message" : "해당 이메일로 이미 가입된 회원이 있습니다."
}
----
----
{
  "code" : "UNAVAILABLE_USERNAME",
  "message" : "사용 불가능한 아이디입니다."
}
----
----
{
  "code" : "EXISTING_USERNAME",
  "message" : "이미 사용중인 아이디입니다."
}
----
